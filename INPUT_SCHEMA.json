{
  "title": "Cyprus Hotel Site Snapshot Input",
  "description": "Collect raw HTML and mobile screenshots (home page) for a single hotel website.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "hotelId": {
      "title": "Hotel ID",
      "description": "MongoDB hotels._id as string. Used to join snapshot with your database.",
      "type": "string",
      "editor": "textfield"
    },
    "domain": {
      "title": "Domain",
      "description": "Hotel website domain without protocol (example.com).",
      "type": "string",
      "editor": "textfield"
    },
    "seedUrls": {
      "title": "Seed URLs",
      "description": "Optional list of starting URLs. If empty, https://<domain>/ will be used.",
      "type": "array",
      "editor": "stringList",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "maxPages": {
      "title": "Max pages",
      "description": "Maximum number of pages to crawl (cost control).",
      "type": "integer",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 50
    },
    "maxDepth": {
      "title": "Max depth",
      "description": "Maximum link depth from home page.",
      "type": "integer",
      "editor": "number",
      "default": 2,
      "minimum": 0,
      "maximum": 5
    },
    "collectHtml": {
      "title": "Collect HTML",
      "description": "Store raw HTML for each visited page.",
      "type": "boolean",
      "editor": "checkbox",
      "default": true
    },
    "mobileViewport": {
      "title": "Mobile viewport",
      "description": "Viewport size for mobile rendering.",
      "type": "object",
      "editor": "json",
      "default": {
        "width": 390,
        "height": 844
      }
    },
    "collectDesktop": {
      "title": "Collect desktop screenshot",
      "description": "Also capture desktop home screenshot (1366x768).",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    },
    "consentClickStrategy": {
      "title": "Consent click strategy",
      "description": "How to handle cookie banner on home page.",
      "type": "string",
      "editor": "select",
      "enum": ["none", "minimal"],
      "default": "minimal"
    },
    "timeoutMsPerPage": {
      "title": "Timeout per page (ms)",
      "description": "Playwright navigation timeout per page.",
      "type": "integer",
      "editor": "number",
      "default": 60000,
      "minimum": 15000,
      "maximum": 180000
    }
  },
  "required": ["hotelId", "domain"]
}
