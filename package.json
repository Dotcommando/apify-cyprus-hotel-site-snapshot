{
  "name": "apify-cyprus-hotel-site-snapshot",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "test": "tsc -p tsconfig.test.json && tsc -p tsconfig.test-build.json && node --test dist-tests/tests/**/*.test.js"
  },
  "dependencies": {
    "apify": "3.5.3",
    "crawlee": "3.16.0",
    "playwright": "1.58.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
